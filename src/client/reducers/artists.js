const initialState = [
    {
        text: 'Use Redux1',
        completed: false,
        id: 0
    }
];

export default function artists(state = initialState, action) {
                    return state;

}